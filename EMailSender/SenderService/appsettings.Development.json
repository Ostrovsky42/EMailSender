{
  "EmailSenderSettings": {
    "ConnectionSettings": {
      "Host": "{{HOST}}",
      "Port": "{{PORT}}"
    },
    "AuthSettings": {
      "FromMailAddress": "{{FROM_MAIL_ADDRESS}}",
      "Password": "{{PASSWORD}}"
    },
    "Logging": {
      "LogLevel": {
        "Default": "Information",
        "Microsoft": "Warning",
        "Microsoft.Hosting.Lifetime": "Information"
      }
    }
  }
}
